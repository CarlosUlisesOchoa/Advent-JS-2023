{
  "name": "advent-js-2023",
  "type": "module",
  "author": {
    "name": "Carlos Ochoa",
    "web": "https://carlos8a.com"
  },
  "dependencies": {
    "fs": "0.0.1-security"
  },
  "scripts": {
    "build": "tsc",
    "test:1": "pnpm run build && cross-env NODE_OPTIONS=--experimental-vm-modules jest -i --bail=false ./dist/challenges/challenge-1/challenge.test.js",
    "test:2": "pnpm run build && cross-env NODE_OPTIONS=--experimental-vm-modules jest -i --bail=false ./dist/challenges/challenge-2/challenge.test.js",
    "test:3": "pnpm run build && cross-env NODE_OPTIONS=--experimental-vm-modules jest -i --bail=false ./dist/challenges/challenge-3/challenge.test.js",
    "test:4": "pnpm run build && cross-env NODE_OPTIONS=--experimental-vm-modules jest -i --bail=false ./dist/challenges/challenge-4/challenge.test.js",
    "test:5": "pnpm run build && cross-env NODE_OPTIONS=--experimental-vm-modules jest -i --bail=false ./dist/challenges/challenge-5/challenge.test.js",
    "test:6": "pnpm run build && cross-env NODE_OPTIONS=--experimental-vm-modules jest -i --bail=false ./dist/challenges/challenge-6/challenge.test.js"
  },
  "devDependencies": {
    "@types/jest": "^29.5.10",
    "@types/node": "^20.10.1",
    "cross-env": "^7.0.3",
    "eslint-config-prettier": "^9.1.0",
    "jest": "^29.7.0",
    "prettier": "^3.1.0",
    "ts-jest": "^29.1.1",
    "ts-standard": "^12.0.2",
    "typescript": "^5.3.2"
  }
}
